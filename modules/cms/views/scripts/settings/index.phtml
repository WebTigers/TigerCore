<?php
// SPDX-License-Identifier: BSD-3-Clause
// Copyright (c) 2026 WebTigers. Tiger™ and WebTigers™ are trademarks of WebTigers.
/**
 * CMS Settings — the site name + the home page served at "/". ViewHelper-only form (the view owns
 * markup). The home page is ANY valid path: a TigerCombobox over Cms_Service_Paths supplies the
 * convenience list (with an "Advanced Search" toggle for the full litterbox), and a dev can type any
 * route freely — the hidden `home_page` field holds whatever is committed. Saving is an /api call to
 * Cms_Service_Settings; behaviour lives in cms.settings.js (no inline script).
 */
$form = $this->form;
$el = function ($name) use ($form) { return $form->getElement($name); };
?>
<div class="d-flex align-items-center justify-content-between mb-4">
    <div>
        <h1 class="h3 mb-1"><?= $this->t('cms.settings.heading') ?></h1>
        <p class="text-body-secondary mb-0"><?= $this->t('cms.settings.subtitle') ?></p>
    </div>
    <div class="text-nowrap">
        <button type="button" id="cms-settings-save" class="btn btn-primary"><i class="fa-solid fa-floppy-disk me-2"></i><?= $this->t('cms.action.save') ?></button>
    </div>
</div>

<div id="cms-settings-feedback"></div>

<form id="cms-settings-form" onsubmit="return false;" novalidate>
    <?= $el('_csrf') ?>
    <div class="row g-4">
        <div class="col-lg-7">
            <div class="card">
                <div class="card-header fw-semibold"><i class="fa-solid fa-globe me-2"></i><?= $this->t('cms.settings.card_site') ?></div>
                <div class="card-body">
                    <div class="mb-3">
                        <label class="form-label" for="<?= $this->escape($el('site_name')->getId()) ?>"><?= $this->t('cms.settings.label_site_name') ?></label>
                        <?= $el('site_name') ?>
                        <div class="form-text"><?= $this->t('cms.settings.help_site_name') ?></div>
                    </div>
                    <div class="mb-0">
                        <label class="form-label" for="set-home-page-search"><?= $this->t('cms.settings.label_home_page') ?></label>

                        <?php // The committed value the form submits — hidden; the combobox writes into it. ?>
                        <div class="d-none"><?= $el('home_page') ?></div>

                        <?php // The searchable "pick or type" control (TigerCombobox → Cms_Service_Paths). ?>
                        <div data-tg-combobox data-service="cms/paths/search"
                             data-value-field="set-home-page" data-advanced-field="set-home-advanced">
                            <input type="text" class="form-control" id="set-home-page-search" data-tg-combobox-search
                                   autocomplete="off" role="combobox" aria-expanded="false" aria-autocomplete="list"
                                   value="<?= $this->escape($this->homeLabel) ?>"
                                   placeholder="<?= $this->escape($this->t('cms.settings.home_placeholder')) ?>"
                                   data-empty-text="<?= $this->escape($this->t('cms.settings.home_empty')) ?>">
                        </div>

                        <div class="form-check mt-2">
                            <input class="form-check-input" type="checkbox" id="set-home-advanced">
                            <label class="form-check-label" for="set-home-advanced"><?= $this->t('cms.settings.advanced_search') ?></label>
                            <div class="form-text mt-0"><?= $this->t('cms.settings.advanced_search_help') ?></div>
                        </div>

                        <div class="form-text"><?= $this->t('cms.settings.help_home_page', '<code>/</code>') ?></div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</form>

<?php
$this->i18n([
    'fixFields' => 'cms.js.fix_fields',
    'networkError' => 'cms.js.network_error',
    'settingsSaved' => 'cms.js.settings_saved',
]);
$this->pageScript($this->themeAssets . '/js/tiger.combobox.js');
$this->pageScript($this->themeAssets . '/js/cms.settings.js');
?>
