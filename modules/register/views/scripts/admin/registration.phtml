<?php
// SPDX-License-Identifier: BSD-3-Clause
// Copyright (c) 2026 WebTigers. Tiger™ and WebTigers™ are trademarks of WebTigers.
/** Settings → Registration — a full-page view of the optional registration. Plain strings (no _t helper). */
$s = $this->state;
?>
<div class="d-flex align-items-center justify-content-between mb-4">
    <div>
        <h1 class="h3 mb-1">Registration</h1>
        <p class="text-body-secondary mb-0">Optional. Register this site for a verified Site ID and to join the Tiger network — it turns nothing on or off.</p>
    </div>
</div>

<div id="reg-feedback"></div>

<div class="row g-4"><div class="col-lg-8"><div class="card">
    <div class="card-header fw-semibold"><i class="fa-solid fa-id-badge me-2"></i>Status</div>
    <div class="card-body">
        <?php if ($s['verified']): ?>
            <span class="badge text-bg-success">Verified</span>
            <dl class="row mt-3 mb-0 small">
                <dt class="col-sm-3 text-body-secondary">Domain</dt><dd class="col-sm-9"><?= htmlspecialchars((string) $s['domain']) ?></dd>
                <dt class="col-sm-3 text-body-secondary">Email</dt><dd class="col-sm-9"><?= htmlspecialchars((string) $s['email']) ?></dd>
                <dt class="col-sm-3 text-body-secondary">Site ID (TSID)</dt><dd class="col-sm-9"><code><?= htmlspecialchars((string) $s['tsid']) ?></code></dd>
            </dl>
        <?php elseif (!$s['started']): ?>
            <span class="badge text-bg-secondary">Not registered</span>
            <p class="text-body-secondary mt-2 mb-3">
                Registering verifies your <strong>domain</strong> (served automatically — nothing to upload) and your
                <strong>email</strong>. We only ever share your domain, this email, and your Tiger/PHP versions. Don’t want to?
                Leave it — or switch off the Registration widget / deactivate this module.
            </p>
            <form id="reg-form" onsubmit="return false;" novalidate>
                <?= $this->form->getElement('_csrf') ?>
                <label class="form-label" for="reg-email">Admin email</label>
                <div class="input-group" style="max-width:28rem;">
                    <?= $this->form->getElement('email') ?>
                    <button type="button" id="reg-register" class="btn btn-primary"><i class="fa-solid fa-check me-2"></i>Register</button>
                </div>
            </form>
        <?php else: ?>
            <span class="badge <?= $s['domain_verified'] ? 'text-bg-success' : 'text-bg-warning' ?>">Domain <?= $s['domain_verified'] ? 'verified' : 'pending' ?></span>
            <span class="badge <?= $s['email_verified'] ? 'text-bg-success' : 'text-bg-warning' ?> ms-1">Email <?= $s['email_verified'] ? 'verified' : 'pending' ?></span>
            <div class="mt-3 d-flex flex-wrap gap-2">
                <?php if (!$s['domain_verified']): ?>
                    <button type="button" id="reg-verify-domain" class="btn btn-outline-primary"><i class="fa-solid fa-globe me-2"></i>Verify domain</button>
                <?php endif; ?>
                <?php if (!$s['email_verified']): ?>
                    <button type="button" id="reg-resend" class="btn btn-outline-secondary"><i class="fa-solid fa-envelope me-2"></i>Resend verification email</button>
                <?php endif; ?>
            </div>
        <?php endif; ?>
    </div>
</div></div></div>

<script>
(function () {
    var fb = document.getElementById('reg-feedback');
    function call(method, btn, extra) {
        var fd = new URLSearchParams(extra || {});
        fd.set('module', 'register'); fd.set('service', 'registration'); fd.set('method', method);
        var csrf = document.querySelector('#reg-form [name="_csrf"]');
        if (csrf) { fd.set('_csrf', csrf.value); }
        return TigerButton.run(btn, function () {
            return fetch('/api', { method: 'POST', headers: { 'X-Requested-With': 'XMLHttpRequest' }, body: fd })
                .then(function (r) { return r.json().catch(function () { return {}; }); });
        }).then(function (res) {
            if (res && res.result === 1) { location.reload(); return; }
            if (res && res.form) { Object.keys(res.form).forEach(function (f) {
                var el = document.querySelector('[name="' + f + '"]'); if (el) { el.classList.add('is-invalid'); }
            }); }
            (res && res.messages || []).forEach(function (m) { TigerDOM.notify(fb, m.message, { type: m.class }); });
        }).catch(function () { TigerDOM.notify(fb, 'Network error — please try again.', { type: 'error' }); });
    }
    function on(id, fn) { var el = document.getElementById(id); if (el) { fn(el); } }
    on('reg-register', function (b) { b.addEventListener('click', function () {
        var e = document.getElementById('reg-email'); call('register', b, { email: e ? e.value : '' });
    }); });
    on('reg-verify-domain', function (b) { b.addEventListener('click', function () { call('verifyDomain', b); }); });
    on('reg-resend', function (b) { b.addEventListener('click', function () { call('resendEmail', b); }); });
})();
</script>
