; ==============================================================================
;  TIGER-OWNED - DO NOT EDIT. Ships in the framework and is REPLACED on every
;  update (composer update / Module Manager) -- any edit here is SILENTLY LOST.
;  Customize by OVERRIDING in an APP-OWNED tier (merges on top, later wins, and
;  survives updates): application/configs/application.ini * local.ini (secrets) *
;  the `config` DB table (runtime, no deploy).  ACL rules: your OWN module's
;  acl.ini or the acl_* DB tables.  See ARCHITECTURE.md Sec 0 + Sec 5 (cascade).
; ==============================================================================

; TigerSEO ACL — /sitemap.xml and /robots.txt are PUBLIC (crawlers hit them unauthenticated), so the
; guest role is allowed both controllers. Deny-by-default means a route with no rule is refused, so these
; explicit guest allows are what make the two routes reachable at all. (The head plugin needs no rule —
; it's a front-controller plugin, not a controller/service.)
[production]

acl.resources.seo_sitemap_ctrl.resource = "Seo_SitemapController"
acl.resources.seo_robots_ctrl.resource  = "Seo_RobotsController"
acl.resources.seo_llms_ctrl.resource    = "Seo_LlmsController"

acl.rules.seo_sitemap_guest.role       = "guest"
acl.rules.seo_sitemap_guest.resource   = "Seo_SitemapController"
acl.rules.seo_sitemap_guest.permission = "allow"

acl.rules.seo_robots_guest.role       = "guest"
acl.rules.seo_robots_guest.resource   = "Seo_RobotsController"
acl.rules.seo_robots_guest.permission = "allow"

acl.rules.seo_llms_guest.role       = "guest"
acl.rules.seo_llms_guest.resource   = "Seo_LlmsController"
acl.rules.seo_llms_guest.permission = "allow"

; Social Cards (admin+, deny-by-default) — the screen that authors the OG title/description/image
; for the public VIEW pages, and its /api service. The service is the AI-reachable half: because the
; agent's tools ARE the role-filtered /api surface, this one allow rule is what makes seo/social/pages
; + seo/social/save drivable by the in-platform agent and over MCP — with exactly the admin's reach,
; never more. Separate resources so the read/write surface can be granted apart from the screen.
acl.resources.seo_admin_ctrl.resource = "Seo_AdminController"
acl.resources.seo_social_svc.resource = "Seo_Service_Social"

acl.rules.seo_admin_ctrl.role       = "admin"
acl.rules.seo_admin_ctrl.resource   = "Seo_AdminController"
acl.rules.seo_admin_ctrl.permission = "allow"

acl.rules.seo_social_svc.role       = "admin"
acl.rules.seo_social_svc.resource   = "Seo_Service_Social"
acl.rules.seo_social_svc.permission = "allow"

[staging : production]

[testing : production]

[development : production]
